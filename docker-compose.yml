version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tradecore-app
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - SPRING_PROFILES_ACTIVE=dev
      - SERVER_PORT=8080
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/tradecore
      - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER:-tradecore}
      - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD:-tradecore123}
      - JAVA_OPTS=-XX:+UseG1GC -XX:MaxRAMPercentage=75.0 -XX:+UseStringDeduplication -XX:-UseAdaptiveSizePolicy
    ports:
      - "8080:8080"
    networks:
      - processor-network
    restart: on-failure
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8080/actuator/health" ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s

  postgres:
    image: postgres:15-alpine
    container_name: tradecore-postgres
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-tradecore}
      - POSTGRES_USER=${POSTGRES_USER:-tradecore}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-tradecore123}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - processor-network
    restart: on-failure
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-tradecore}" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # redis:
  #   image: redis:7-alpine
  #   container_name: tradecore-redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   networks:
  #     - processor-network
  #   restart: on-failure
  #   healthcheck:
  #     test: [ "CMD", "redis-cli", "ping" ]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

networks:
  processor-network:
    driver: bridge

volumes:
  postgres_data:
  # redis_data:
